export default class Tile{
    point: number;

    x: number;
    y: number;

    init(): void{

    }

    constructor(point: number, y: number, x: number){
        this.init();
        this.point = point;
        this.x = x;
        this.y = y;
    }



}